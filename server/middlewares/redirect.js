// 'use strict';

// var bird = require('bluebird');
// var self = module.exports;

// self.exports = function(req, res, next) {
// 	if (!req.isCreateToken) {
// 		return res._redirect('page.404');
// 	}

// 	return res._redirect('page.default');
// };
